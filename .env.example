# intent-gate 零配置即可使用（single 通道，对话框兜底，钉钉一概不碰）。
# 本文件只在你想显式调整时才需要 cp .env.example .env。

# 项目根目录（.harness 所在）；MCP stdio 被 agent 在项目根拉起时保持默认即可
HG_WORKSPACE_ROOT=.

# HG_CHANNEL 只支持 single（默认）。
# ⚠️ 旧版的 HG_CHANNEL=group 已剥离：钉钉群通道与决策闸门迁至姊妹篇
# intent-gate-service（独立 MCP 服务），凭据配置见 intent-gate-service/.env.example。
# 在本插件的 .env 里留 HG_CHANNEL=group 会直接启动失败并提示迁移。

HG_LOG_LEVEL=INFO
